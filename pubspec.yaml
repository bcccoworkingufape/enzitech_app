name: enzitech_app
description: Aplicativo para Experimento e Análise de Solo.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 0.6.0+13

environment:
  sdk: ^2.19.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  auto_size_text: ^3.0.0
  connectivity_plus: ^5.0.2
  cupertino_icons: ^1.0.5
  curl_logger_dio_interceptor: ^1.0.0
  dartz: ^0.10.1
  data_table_2: ^2.4.1
  dio: ^5.4.0
  excel: ^4.0.0
  firebase_core: ^2.24.2
  firebase_crashlytics: ^3.4.8
  flutter:
    sdk: flutter
  flutter_expandable_fab: ^2.0.0
  flutter_file_dialog: ^3.0.2
  flutter_form_builder: ^9.1.1
  flutter_localizations:
    sdk: flutter
  flutter_svg: ^2.0.9
  flutter_svg_provider: ^1.0.7
  form_builder_validators: ^9.1.0
  get_it: ^7.2.0
  google_fonts: ^6.1.0
  group_button: ^5.2.2
  internet_connection_checker: ^1.0.0+1
  intl: ^0.18.1
  package_info_plus: ^5.0.1
  path_provider: ^2.0.14
  percent_indicator: ^4.2.2
  phosphor_flutter: ^2.0.1
  pretty_dio_logger: ^1.2.0-beta-1
  share_plus: ^7.2.1
  shared_preferences: ^2.0.17
  toggle_switch: ^2.0.1
  url_launcher: ^6.1.9

dev_dependencies:
  build_runner: ^2.3.3
  flutter_lints: ^3.0.1
  flutter_native_splash: ^2.2.16
  flutter_test:
    sdk: flutter
  icons_launcher: ^2.0.6
  import_path_converter: ^1.0.1
  import_sorter: ^4.6.0
  mockito: ^5.4.0
  test: ^1.22.0

flutter_native_splash:
  color: "#006D39"
  color_dark: "#00391B"
  image: assets/launcher/native_splash.png
  image_dark: assets/launcher/native_splash_dark.png
  android: true
  ios: true

  android_12:
    image: assets/launcher/foreground_icon.png
    icon_background_color: "#006D39"
    image_dark: assets/launcher/foreground_icon_dark.png
    icon_background_color_dark: "#00391B"

icons_launcher:
  image_path: "assets/launcher/icon.png"
  platforms:
    android:
      enable: true
      image_path: "assets/launcher/icon.png"
      adaptive_background_color: "#006D39"
      adaptive_foreground_image: "assets/launcher/foreground_icon.png"
    ios:
      enable: true
      image_path: "assets/launcher/icon.png"

import_path_converter:
  relative: true
  ignored_files:
    - /lib/shared/ui/widgets/full_widgets_override/*
    - /test/*

import_sorter:
  emojis: true
  comments: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/svgs/
    - assets/svgs/light/
    - assets/svgs/dark/
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
