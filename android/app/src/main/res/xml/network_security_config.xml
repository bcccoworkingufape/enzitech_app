<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Default policy: HTTPS only, system trust store only. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
        Legacy allow-list: the production backend currently serves over
        HTTP at 200.133.6.201. This exception must be removed once the
        backend ships TLS. See docs/security/security_scenario1_checklist.md.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">200.133.6.201</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
